# Scenes

Use this folder for React-authored scene orchestration that sits above Spline or React Three Fiber primitives.

- `HeroScene`: fullscreen hero composition, scroll camera, Spline fallback.
- `FeatureScene`: product visualization and landmark nodes.
- `AmbientScene`: global background particles and gradient lighting.

