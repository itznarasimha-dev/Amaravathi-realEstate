# 3D Components

- `SplineStage`: lazy Spline loader with a procedural fallback.
- `AmbientScene`: global atmospheric particles and lighting.
- Future R3F components should keep canvas rendering isolated and lazy loaded.

